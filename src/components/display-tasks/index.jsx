// import EachTask from '../each-task'
import './style.css'

const DisplayTasks = (props) => {
	// props.tasks.forEach((element) => {
	// 	console.log('the tasks are' + element)
	// })
	return <div className='DisplayTasks'></div>
}

export default DisplayTasks
