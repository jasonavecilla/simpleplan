// import HomeButtons from './../home-buttons/index'
import './style.css'

const NavBar = () => {
	return (
		<div className='NavBar'>
			{/* this is the navbar with an add button, the edit button will be on the task
            class, but they both display the add-edit page */}
			{/* {homeButtons.map((items) => (
				<HomeButtons key={items} className='NavBar__inactive' name={items} />
			))} */}
		</div>
	)
}

export default NavBar
